# 운영 메모 — 한도·남용·비용

공개 서비스로 열기 전에 확인한 내용입니다. 2026-09-16 기준 (대량 전송 대비 후).

## 지금 걸려 있는 한도

| 항목 | 값 | 위치 |
| --- | --- | --- |
| 파일 1개 | 20MB | `MAX_FILE_MB` |
| 세션 1개 | 150MB / 120장 | `MAX_SESSION_MB`, `MAX_ITEMS` |
| 그중 원본 화질 | 20장 | `MAX_ORIGINAL_ITEMS` |
| 서버 전체 보관량 | 500MB | `MAX_TOTAL_MB` |
| 세션 수명 | 30분 (저장하면 즉시 삭제) | `SESSION_TTL_MIN` |
| IP당 세션 생성 | 시간당 30개 | `SESSIONS_PER_IP_HOUR` |
| IP당 업로드 | 하루 3GB | `UPLOAD_GB_PER_IP_DAY` |
| 하루 전송량 | 6GB (월 $30 예산) | `DAILY_EGRESS_GB` |
| 컨테이너 | 1Gi, 동시 처리 10, 인스턴스 1개 | 배포 파라미터 |

한 사람이 100장(축소 모드 약 80MB)을 보내는 것이 기준 시나리오입니다.

## 비용 구조

비용은 대부분 **PC로 내려가는 전송량**에서 나옵니다. 올리는 트래픽은 무료입니다.

| 항목 | 단가 | 무료 한도 |
| --- | --- | --- |
| Firebase Hosting 전송 | $0.15/GB | 하루 360MB |
| Cloud Run 연산 (서울, Tier 2) | vCPU-초 $0.0000336 / GiB-초 $0.0000035 | 월 180,000 vCPU-초 |
| Cloud Run 요청 | 100만 건당 $0.40 | 월 200만 건 |

100장 1건에 드는 전송량:

| | 미리보기 | 저장 | 합계 |
| --- | --- | --- | --- |
| 폰이 만든 썸네일 사용 (현재) | 약 2MB | 약 80MB | **약 82MB** |
| 원본을 미리보기로 쓰던 예전 방식 | 약 80MB | 약 80MB | 약 160MB |

| 사용량 | 월 예상 비용 |
| --- | --- |
| 하루 10건 | 약 $2~3 |
| 하루 50건 | 약 $16 (하루 한도 6GB에 근접) |
| 하루 73건 이상 | 하루 전송 한도에 걸려 새 세션이 막힘 |

`DAILY_EGRESS_GB` 를 올리면 그만큼 비용도 올라갑니다. 1GB 늘릴 때 월 약 $4.5입니다.

## 위험 목록과 대응

### 1. 다른 사용자의 세션이 삭제된다 — 완화됨

전체 보관량이 차면 오래된 세션부터 버리는 것은 그대로입니다. 다만,

- **저장이 끝난 파일은 바로 지웁니다.** 메모리를 붙잡는 시간이 30분에서 수십 초로 줄었습니다.
- **버릴 세션을 고르는 순서를 바꿨습니다.** 1분 넘게 조용한 세션을 먼저 고르고, 그래도 모자랄
  때만 최근 세션에 손댑니다. 지금 사진을 올리는 중인 세션은 어떤 경우에도 건드리지 않습니다.
- 세션을 버릴 때마다 로그를 남기므로 Cloud Run 로그에서 `메모리 확보를 위해` 로 검색하면 됩니다.

### 2. 메모리 초과로 컨테이너가 재시작된다 — 완화됨

`--memory 1Gi --concurrency 10` 이 전제입니다. 보관량 500MB + 동시 업로드 10 × 20MB + 여유분입니다.
**배포할 때 이 두 옵션을 빠뜨리면 이 계산이 깨집니다.** README의 배포 명령을 그대로 쓰세요.

### 3. 60초 타임아웃 — 일부 남음

Firebase Hosting은 요청 하나를 60초에서 끊습니다. 100장을 ZIP 하나로 받으면 80~100MB라
느린 회선에서는 걸릴 수 있습니다. PC 화면에서 60MB가 넘으면 「받는 즉시 자동 저장」을
권하는 확인창을 띄웁니다. 한 장씩 받으면 요청도 한 장씩이라 타임아웃과 무관합니다.

### 4. 비용이 통제되지 않는다 — 대응됨

- IP당 세션 생성(시간당 30개)과 업로드량(하루 3GB) 제한.
- 하루 전송량이 `DAILY_EGRESS_GB` 를 넘으면 **새 세션만** 막습니다. 이미 사진을 보내는
  중인 사람은 끝까지 마칩니다.
- [ ] **결제 예산 알림은 아직 설정 전입니다.** GCP 콘솔 → 결제 → 예산 및 알림에서
      월 $30, 알림 50/90/100%로 잡아 두세요. 코드가 막지 못하는 경우(예: 공격)를
      가장 먼저 알아채는 수단입니다.

### 5. 임시 파일 배포처로 악용될 수 있다 — 일부 남음

`/api/file/:id/:fileId` 는 주소만 알면 누구나 열립니다. 세션 ID가 12자 랜덤이라 남이
추측하는 것은 사실상 불가능하지만, 올린 사람이 스스로 주소를 퍼뜨리면 30분짜리 익명
파일 배포처가 됩니다. 저장 후 자동 삭제로 노출 시간이 짧아졌지만, 저장하지 않으면
여전히 30분입니다. 하단 고지에 금지 사항을 적어 두었습니다.

## 남은 일

- [ ] 결제 예산 알림 (위 4번)
- [ ] 개인정보처리방침·이용약관 페이지 — 하단 링크가 비어 있습니다.
      애드센스 심사에서도 보통 요구합니다. (`banners.json` 의 `site.groups`)
- [ ] 동시 사용자 확장 — 인스턴스를 늘리려면 세션 저장소를 Firestore/Redis로,
      파일 본문을 Cloud Storage로 빼야 합니다. 원본 화질 대량 전송도 그때 가능해집니다.

## 상태 확인

```bash
curl -s https://phone2pc.iuser.kr/api/stats
```

`heldMB`(보관 중), `egressTodayMB`(오늘 전송량), `rssMB`(컨테이너 메모리)를 봅니다.
`STATS_TOKEN` 환경변수를 설정하면 `?token=…` 이 맞아야 열립니다.

## 참고

배포 방법과 파일 구조는 [README.md](README.md)를 보세요.
