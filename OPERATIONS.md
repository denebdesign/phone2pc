# 운영 메모 — 남용 위험과 대응

공개 서비스로 열기 전에 확인한 내용입니다. 2026-09-09 기준.

## 지금 걸려 있는 한도

| 항목 | 값 | 위치 |
| --- | --- | --- |
| 파일 1개 | 20MB | `MAX_FILE_MB` |
| 세션 1개 | 80MB / 40장 | `MAX_SESSION_MB`, `MAX_ITEMS` |
| 서버 전체 | 320MB | `MAX_TOTAL_MB` |
| 세션 수명 | 30분 | `SESSION_TTL_MIN` |
| 컨테이너 | 512Mi, 인스턴스 1개 | 배포 파라미터 |

세션 하나에 1GB를 넣는 것은 막혀 있습니다. 다만 **세션 생성 자체에는 제한이 없어서**,
「새 QR 만들기」를 반복하면 얼마든지 많은 양을 옮길 수 있습니다.

## 위험 목록 (심각한 순서)

### 1. 다른 사용자의 세션이 삭제된다

전체 320MB가 차면 `makeRoom()`이 오래된 세션부터 통째로 버립니다.
한 사람이 상한을 채우는 순간, 마침 사진을 보내던 다른 사용자의 파일이
PC에 뜨기도 전에 사라집니다. 인스턴스가 1개라 모든 사용자가 같은 메모리를 씁니다.

### 2. 메모리 초과로 컨테이너가 재시작된다

`express.raw`는 업로드 본문을 메모리에 전부 받은 뒤 핸들러를 호출합니다.
Cloud Run 기본 동시 처리 수는 80이므로, 동시 업로드가 겹치면
파일 데이터 320MB에 더해 요청당 최대 20MB가 추가로 쌓입니다.
512Mi를 넘기면 컨테이너가 강제 재시작되고 **모든 세션이 한꺼번에 사라집니다.**

### 3. 60초 타임아웃

Firebase Hosting은 요청 하나를 60초에서 끊습니다 (Cloud Run의 120초 설정보다 먼저 걸립니다).
원본 화질로 20MB를 느린 회선에서 올리거나, 「전체 ZIP 저장」으로 80MB를 내려받을 때 걸릴 수 있습니다.
남용이 아니라 평범한 사용자가 먼저 만나는 문제입니다.

### 4. 비용이 통제되지 않는다

Firebase Hosting 전송량은 하루 360MB까지 무료, 초과분은 GB당 약 $0.15입니다.
로그인이 없어 누가 얼마나 쓰는지 통제할 수 없고, 스크립트로 자동화하면 하루 수백 GB도 가능합니다.

| 하루 전송량 | 대략 월 비용 |
| --- | --- |
| 1GB | 거의 0 |
| 10GB | 약 $40 |
| 50GB | 약 $220 |

### 5. 임시 파일 배포처로 악용될 수 있다

`/api/file/:id/:fileId` 는 주소만 알면 누구나 열립니다.
세션 ID가 12자 랜덤이라 남이 추측해서 훔쳐보는 것은 사실상 불가능하지만,
올린 사람이 스스로 주소를 퍼뜨리면 30분짜리 익명 파일 배포처가 됩니다.
불법 콘텐츠가 여기를 거쳐 가면 도메인 소유자가 대응해야 합니다.

## 대응 상태

- [x] **하단 고지 문구 정정** — 사진이 "PC가 받아가면 즉시" 지워진다고 적혀 있었으나,
      실제로는 「비우기」를 누르거나 30분이 지나야 지워집니다. 실제 동작에 맞게 고쳤습니다.
- [x] **이용 제한 안내 추가** — 세션 주소를 아는 사람은 파일을 열 수 있다는 점과,
      위법·권리침해 파일 전송 금지를 하단에 표시했습니다. (`banners.json`의 `site.notice`)
- [ ] **결제 예산 알림** — GCP 콘솔 → 결제 → 예산 및 알림. 월 $10 정도로 설정.
      비용 이상을 가장 먼저 알아채는 수단이라 우선순위가 가장 높습니다.
- [ ] **동시 처리 수 제한** — 배포 시 `--concurrency 20 --memory 1Gi` 추가. 위험 2 완화.
- [ ] **IP당 세션 생성 제한** — 시간당 20개 정도. 위험 1·4·5를 한 번에 줄입니다.
- [ ] **다운로드 후 자동 삭제** — 위험 1을 줄이고 고지 문구와 동작을 더 가깝게 만듭니다.
- [ ] **`MAX_TOTAL_MB` 200 / `MAX_FILE_MB` 15로 하향** — 위험 2·3 완화.
- [ ] **개인정보처리방침·이용약관 페이지** — 하단 링크가 비어 있습니다.
      애드센스 심사에서도 보통 요구합니다. (`banners.json`의 `site.groups`)

## 참고

배포 방법과 파일 구조는 [README.md](README.md)를 보세요.
